{
  "name": "help-desk-ticketing-system-demo",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && node index.js",
    "dev": "concurrently --kill-quit "npm run client" "npm run server"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}